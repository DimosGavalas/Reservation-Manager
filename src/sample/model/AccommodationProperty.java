package sample.model;

import java.util.ArrayList;
import java.util.HashMap;

public class AccommodationProperty {
    // HashMap that holds the room types and the room number representing each type. Each key represents
    // a room type. Each key contains a list of room numbers
    private HashMap<String, ArrayList<Integer>> propertyRooms;
}
